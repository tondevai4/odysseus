# Development and testing dependencies — NOT for production installs.
# Install with: pip install -r requirements-dev.txt

# Testing
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0

# HTTP test client
httpx>=0.27
httpx2

# Type checking
mypy>=1.10

# Linting / formatting
ruff>=0.5

# CSS linting (via npm, listed here for documentation)
# npm install --save-dev stylelint stylelint-config-standard
